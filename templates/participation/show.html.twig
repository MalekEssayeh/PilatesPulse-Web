{% extends 'base.html.twig' %}

{% block title %}Participation{% endblock %}

{% block body %}
    <h1>Participation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participation.id }}</td>
            </tr>
            <tr>
                <th>Event_id</th>
                <td>{{ participation.eventId }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ participation.userId }}</td>
            </tr>
            <tr>
                <th>ParticipationDate</th>
                <td>{{ participation.participationDate ? participation.participationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_participation_index') }}">back to list</a>

    <a href="{{ path('app_participation_edit', {'id': participation.id}) }}">edit</a>

    {{ include('participation/_delete_form.html.twig') }}
{% endblock %}
