{#


{% extends 'base.html.twig' %}

{% block title %}Shoppingcart index{% endblock %}

{% block body %}
    <h1>Shoppingcart index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idshoppingcart</th>
                <th>Iduser</th>
                <th>Nameproduct</th>
                <th>Image</th>
                <th>Productdescription</th>
                <th>Priceproduct</th>
                <th>Quantity</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for shoppingcart in shoppingcarts %}
            <tr>
                <td>{{ shoppingcart.idshoppingcart }}</td>
                <td>{{ shoppingcart.iduser }}</td>
                <td>{{ shoppingcart.nameproduct }}</td>
                <td>{{ shoppingcart.image }}</td>
                <td>{{ shoppingcart.productdescription }}</td>
                <td>{{ shoppingcart.priceproduct }}</td>
                <td>{{ shoppingcart.quantity }}</td>
                <td>
                    <a href="{{ path('app_shoppingcart_show', {'idshoppingcart': shoppingcart.idshoppingcart}) }}">show</a>
                    <a href="{{ path('app_shoppingcart_edit', {'idshoppingcart': shoppingcart.idshoppingcart}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_shoppingcart_new') }}">Create new</a>
{% endblock %}



#}

{% extends 'base.html.twig' %}

{% block title %}Shoppingcart index{% endblock %}

{% block body %}
    <br>
    <h1>Shoppingcart index</h1>
    <br>
    {% if app.session.flashBag.has('success') %}
        <div class="alert alert-success alert-dismissible fade show">
            {% for message in app.flashes('success') %}
                {{ message|trans }}
            {% endfor %}

        </div>
        <script>
            //5 sec bech thidde
            document.addEventListener("DOMContentLoaded", function() {
                const alert = document.querySelector('.alert');
                alert.classList.add('show');
                setTimeout(function() {
                    alert.classList.remove('show');
                }, 5000);
            });
        </script>
    {% endif %}
    <table class="table">
    <thead>
    <tr>
        <th>Image</th>
        <th>Nameproduct</th>
        <th>Productdescription</th>
        <th>Priceproduct</th>
        <th>Quantity</th>
        <th>actions</th>
    </tr>
    </thead>
    <tbody>
    {% for shoppingcart in shoppingcarts %}
        <tr>
            <td><img src="{{ asset('uploads/' ~ shoppingcart.image) }}" class="card__image" width="250" height="200"></td>
            <td>{{ shoppingcart.nameproduct }}</td>
            <td>{{ shoppingcart.productdescription }}</td>
            <td>{{ shoppingcart.priceproduct }}</td>
            <td>{{ shoppingcart.quantity }}</td>
            <td>
                {{ include('shoppingcart/_delete_form.html.twig') }}
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="8">no records found</td>
        </tr>
    {% endfor %}
    </tbody>
    </table>


{% endblock %}
